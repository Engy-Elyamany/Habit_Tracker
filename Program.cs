using HabitTracker;
using HabitTracker.Models;
using HabitTracker.Services;
using HabitTracker.UI;
internal class Program
{
    private static void Main(string[] args)
    {
        var App = new HabitApp();
        App.Run();
    }
}
